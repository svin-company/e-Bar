<Window x:Class="eBar.KitchenDisplayApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:eBar.KitchenDisplayApp"
        mc:Ignorable="d"
        Title="MainWindow"
        MinHeight="450"
        MinWidth="800"
        Background="#202020"
        WindowStartupLocation="CenterOwner">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition  />
            <ColumnDefinition  />
            <ColumnDefinition  />
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      x:Name="MainScrollViewer">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition  />
                    <ColumnDefinition Width="50" />
                </Grid.ColumnDefinitions>

                <Grid HorizontalAlignment="Left" Background="#404040" Grid.Column="0">
                    <StackPanel
                        VerticalAlignment="Top"
                        x:Name="LeftStackPanel"
                        Orientation="Vertical"
                        HorizontalAlignment="Left" />
                </Grid>

                <Grid HorizontalAlignment="Left"  Grid.Column="1">
                    <StackPanel
                        HorizontalAlignment="Left"
                        x:Name="MiddleStackPanel"
                        VerticalAlignment="Top" />
                </Grid>

                <Grid HorizontalAlignment="Right"  Background="#404040" Grid.Column="2">
                    <StackPanel
                        VerticalAlignment="Top"
                        x:Name="RightStackPanel"
                        Orientation="Vertical"
                        HorizontalAlignment="Center" />
                </Grid>
            </Grid>
        </ScrollViewer>

        <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Background="#303030">
            <controls:OrderControl
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                LeftTarget="{Binding ElementName=LeftStackPanel}"
                MiddleTarget="{Binding ElementName=MiddleStackPanel}"
                RightTarget="{Binding ElementName=RightStackPanel}"
                ScrollTarget="{Binding ElementName=MainScrollViewer}"/>
        </Grid>
    </Grid>
</Window>