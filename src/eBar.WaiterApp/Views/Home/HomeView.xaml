<Window x:Class="eBar.WaiterApp.Views.Home.HomeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:eBar.WaiterApp.Views.Home"
        mc:Ignorable="d"
        Title="WaiterApp" Height="450" Width="800">
    <StackPanel>
        <TextBlock Text="Приложение для официанта" 
           FontSize="30" 
           TextAlignment="Center" 
           Margin="30" />
        <Button x:Name="newOrderButton" 
        Content="Новый заказ"
        Command="{Binding DataContext.CreateNewOrderCommand, RelativeSource={RelativeSource AncestorType=Window}}"
        Margin="30" 
        Padding="20"
        FontSize="30"/>
        <Button x:Name="ordersListButton" 
        Content="Список заказов"
        Command="{Binding DataContext.OpenOrdersCommand, RelativeSource={RelativeSource AncestorType=Window}}"
        Margin="30" 
        Padding="20" 
        FontSize="30"/>
    </StackPanel>
</Window>
