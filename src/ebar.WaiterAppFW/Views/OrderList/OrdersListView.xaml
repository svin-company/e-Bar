<Window x:Class="ebar.WaiterAppFW.Views.OrderList.OrdersListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ebar.WaiterAppFW.Views.OrderList"
        mc:Ignorable="d"
        Title="OrdersListView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="7*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" 
 Content="Cтол №"
Margin="5"
 HorizontalAlignment="Left"></Label>
            <ComboBox Grid.Column="1"
          x:Name="tableCombobox"
          Width="40" Height="40"
          HorizontalAlignment="Left"
          Margin="5"
          ItemsSource="{Binding Tables}"
          SelectedItem="{Binding SelectedTable, Mode=TwoWay}"
          DisplayMemberPath="Id" />
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <ListView x:Name="OrderListView"
          Margin="5"
          HorizontalAlignment="Left"
          ItemsSource="{Binding OrdersForSelectedTable}"
          SelectedItem="{Binding SelectedOrder, Mode=TwoWay}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Номер" DisplayMemberBinding="{Binding Id}" Width="80"/>
                            <GridViewColumn Header="Дата" DisplayMemberBinding="{Binding OrderTime, StringFormat ='dd/MM/yyyy'}" Width="120"/>
                            <GridViewColumn Header="Статус" DisplayMemberBinding="{Binding OrderStatus}" Width="80"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="5*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <DataGrid Grid.Row="0"
                      Margin="5" 
                     CanUserAddRows="false" 
                     AutoGenerateColumns="False"
                     ItemsSource="{Binding DataContext.SelectedOrder.OrderItems, 
                                RelativeSource={RelativeSource AncestorType=Window}}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="2*" Header="Название" Binding="{Binding Food.Name}"/>
                        <DataGridTextColumn Width="1*" Header="Кол-во" Binding="{Binding Amount}"/>
                        <DataGridTextColumn Width="1*" Header="Стоимость" Binding="{Binding TotalPrice}"/>

                    </DataGrid.Columns>
                </DataGrid>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Column="0" 
                             Content = "{Binding SelectedOrder.OrderStatus}" 
                              ContentStringFormat ="Статус заказа: {0}"/>
                    <Button Grid.Column="1"
                         Content="Cменить статус заказа"
                         Command="{Binding DataContext.ChangeStatusCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                         CommandParameter="{Binding  SelectedOrder}"
                         Margin="10"/>
                </Grid>
            </Grid>

        </Grid>

    </Grid>
</Window>
